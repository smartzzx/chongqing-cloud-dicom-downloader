# 运行时依赖
# 安装：pip install -r requirements.txt
# 已在 Windows + Python 3.13 环境下验证；因 pydicom 3.x 要求，最低需要 Python 3.10

# 异步 HTTP 客户端，负责影像数据下载
# （yarl 是 aiohttp 的传递依赖，会被自动安装，无需在此单独列出）
aiohttp~=3.12

# DICOM 文件的构造、封装与读写
pydicom~=3.0

# 下载进度条
tqdm~=4.67

# 将 DICOM 转成同名 JPG 预览图：像素数组与窗宽窗位映射
numpy~=2.0

# 将 DICOM 转成同名 JPG 预览图：JPEG 编码
pillow~=12.0

# 无头浏览器（优先调用系统 Edge），用于导出电子报告 PDF
playwright~=1.55
